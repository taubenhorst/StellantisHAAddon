"""Minimal stand-in for the `homeassistant` package.

Provides only the symbols imported by the vendored stellantis_vehicles code so
that `stellantis.py`, `const.py` and `utils.py` run unchanged outside of Home
Assistant Core. Nothing here is a general HA replacement.
"""
